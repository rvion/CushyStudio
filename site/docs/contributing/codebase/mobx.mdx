# mobx rules we should try to follow

-   override some lazy getters (getFile, getOrCreateInstance, ...) with `action` instead of `autoaction`
-   react refs stored in state classes must be declared as non observable or observable refs unless necessary
