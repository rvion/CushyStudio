import { Trigger } from '../trigger/Trigger'

export class CmdHelper {
   UNMATCHED = Trigger.UNMATCHED
   Success = Trigger.Success
   FAILED = Trigger.FAILED
}

export const cmdHelper = new CmdHelper()
