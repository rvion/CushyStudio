import { describe, expect, it } from 'bun:test'

describe.skip('type codegen', () => {
    describe('for schema', () => {
        it('works', () => {
            expect('🔴').toBe('🟢')
        })
    })
    describe('for value', () => {
        it('works', () => {
            expect('🔴').toBe('🟢')
        })
    })
    describe('for serial', () => {
        it('works', () => {
            expect('🔴').toBe('🟢')
        })
    })
})
