/* (bird_d)TODO: Add Property Shelf here when rvion adds stuff I need */

import type { PanelCaptioningProps } from './PanelCaptioningUI'

import { observer } from 'mobx-react-lite'

import { ShellInputOnly } from '../../csuite-cushy/shells/ShellInputOnly'
import { ResizableFrame } from '../../csuite/resizableFrame/resizableFrameUI'
import { BasicShelfUI } from '../../csuite/shelf/ShelfUI'

// export const CaptionPropertyShelfUI = observer(function CaptionPropertyShelfUI_(doc) {
//    return (
//       <BasicShelfUI tw='flex flex-col !gap-2 overflow-clip' anchor='right'>
//          <doc.FolderPath.UI Shell={ShellInputOnly} />
//          <ResizableFrame></ResizableFrame>
//       </BasicShelfUI>
//    )
// })
