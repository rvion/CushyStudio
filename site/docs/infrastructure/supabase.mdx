:::info

@rvion personal note;

:::

```bash

# 1. create migration
supabase migration new new-migration

# 2. edit migration
# cmd + click on the migration file to open it
# regular pgsql file

# 3. run migration and update tyeps
supabase db push
supabase gen types typescript --linked src/supa/database.types.ts

```

```sh
# https://github.com/supabase/cli
./node_modules/.bin/supabase login
alias supabase=./node_modules/.bin/supabase
supabase init
# N
supabase link --project-ref hcbawdmupnlvqtyaubvt
# Enter your database password (or leave blank to skip):
# Finished supabase link.
supabase migration new new-migration
supabase db push
```
